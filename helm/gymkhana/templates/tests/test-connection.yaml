apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gymkhana.fullname" . }}-test-connection"
  labels:
{{ include "gymkhana.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "gymkhana.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
