apiVersion: v1
kind: Service
metadata:
  name: {{ include "gymkhana.fullname" . }}
  labels:
    {{- include "gymkhana.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gymkhana.selectorLabels" . | nindent 4 }}
